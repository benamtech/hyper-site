# Validation Report — Graph-Learning Paper Triage

Date: 2026-07-18  
Branch: `agent/ui-metaprogramming-pass-1`  
PR: `#17`  
Reviewed head before this documentation pass: `8fc397a38381474734cfe6e411d1f0bf81b31349`  
Exact-head workflow: `29631422126` — pass  
Scope: supplied WWW 2026 graph papers, graph-learning applicability, production promotion gates, and Wasm separation

## 1. Review method

```text
supplied paper-title list
-> exact title/author/source verification
-> identify studied graph task
-> compare with framework problem class
-> map to validation/pass/fail vectors
-> retain / comparison-only / future / out-of-scope decision
-> implement only conclusions supported by evidence
```

The attached/generated bibliography remained a lead list rather than authority. It explicitly identifies itself as generated by Kimi.ai and mixes verified papers with model-generated action items. The useful harness and structured-generation leads remain governed by `25-academic-crosswalk-agent-harness-structured-generation-and-acceleration.md`.

## 2. Verified sources and disposition

| Paper | Verified source | Actual problem class | Framework disposition |
|---|---|---|---|
| GraphTARIF | arXiv `2510.10631` | scalable learned graph attention/node representation | future supervised comparison only |
| VSAL | arXiv `2602.13880`; DOI `10.1145/3774904.3792224` | vision-based structural graph-property detection | debug/operator visualization lead only |
| Towards Graph Foundation Model | DOI `10.1145/3774904.3792236` | cross-graph node-feature transfer | future multi-project research only |
| A Unified Graph Clustering Network | DOI `10.1145/3774904.3792266` | learned graph clustering | comparison-only after real external labels |
| Sustained Vertex Cover on Temporal Graphs | DOI `10.1145/3774904.3792250` | temporal graph coverage/scheduling | no initial compiler role |
| Dual-level Reweighting for PU Graph Classification | DOI `10.1145/3774904.3792251` | positive-unlabeled graph classification | future review-prioritization experiment only |
| DP-DGAD | arXiv `2508.00664`; DOI `10.1145/3774904.3792268` | temporal graph anomaly detection | future maintenance/drift comparison only |
| E²SGNN | DOI `10.1145/3774904.3792271` | spiking GNN representation efficiency | out of scope |
| Interaction anomaly detection in streaming graphs | DOI `10.1145/3774904.3792282` | streaming behavior anomaly detection | future field-monitoring lead only |
| Graph-to-Tree | DOI `10.1145/3774904.3792286` | self-supervised graph representation | future multi-graph benchmark only |

## 3. Standard validation vector

| Attribute | Validation vector | Pass vector | Fail vector | Finding |
|---|---|---|---|---|
| `graph-method.problem-match` | target task, graph type, static/temporal state, labels | exact measured framework failure matches paper task | generic graph relevance inferred from title | pass: no paper misclassified as current authority |
| `graph-method.data` | real graph count, labels, independent splits, temporal history | sufficient non-agent-authored data exists | synthetic fixture used as training/acceptance truth | fail/pending: required real corpus absent |
| `graph-method.baseline` | lexical, typed graph, connected components, sparse coverage, HRR | method beats named simple baseline on held-out framework metric | architecture novelty substitutes for comparison | pending: no learned method benchmark run |
| `graph-method.safety` | evidence, materiality, exclusions, targeting policy | compiler gates remain hard authority | learned score overrides safety/evidence | pass decision: no promotion allowed |
| `graph-method.tail-noise` | hidden slices, isolated outliers, false admissions | improves tail coverage without noisy-page increase | clustering/diversity admits unsupported points | pending real benchmark |
| `graph-method.auditability` | model/data/version/seed hash, variance, explanation | reproducible comparison artifact | unstable clusters become page truth | pending |
| `graph-method.performance` | complete site-run time/memory/cost | measurable end-to-end benefit | GPU/model microbenchmark only | pending |
| `graph-method.publication` | information gain, cannibalization, browser/crawler, field cohort | independently better pages/outcomes | graph score described as SEO proof | not run |
| `graph-method.wasm-separation` | numeric kernel share, ABI, bridge cost, parity | execution target independently passes acceleration gate | graph paper is used to justify Wasm | pass decision |

## 4. Implementation finding

No graph transformer, GNN, learned clustering model, positive-unlabeled classifier, temporal anomaly model, or graph foundation model was added.

That is the correct implementation result because:

```text
current deterministic path already meets synthetic 10k planning budget
+ current failures are real-data/provider/content validation gaps
+ supplied papers solve different graph tasks
+ no real independent labels exist
+ no method has beaten the named baseline
```

The implementation remains:

```text
Stage-1 agent proposal
-> deterministic provenance/materiality/safety/reviewer approval
-> TF-IDF/BM25 + typed graph + hard constraints
-> bounded sparse opportunity construction
-> sparse corpus selection
-> HRR structural representation
-> Stage-2 page-concept jobs
-> deterministic compiler validation
```

## 5. Wasm finding

The paper set does not alter the Wasm decision.

Graph-learning architecture and execution target are independent variables. The current accepted speedup came from algorithm/data-layout changes. TypeScript remains the semantic and production oracle; Zig/Wasm remains a research arm until the existing bridge-inclusive parity and full-build-savings policy passes.

## 6. Pass/fail result

Pass:

- exact paper identities were verified before use;
- problem-class mismatch is explicit;
- graph methods remain subordinate to evidence and safety;
- connected components remain descriptive rather than semantic truth;
- HRR remains first-class structural representation without becoming page authority;
- the 10,000-page workload remains one finite site-generation run;
- no unjustified GNN, transformer, clustering, temporal model, GPU, or Wasm feature was introduced;
- future promotion has one shared validation/pass/fail gate.

Pending/fail:

- no real multi-project graph corpus;
- no independent segment, relevance, page-quality, drift, or anomaly labels;
- no learned graph comparison against the deterministic baseline;
- no real Stage-1/Stage-2 provider output;
- no 100–500 real noindex page cohort;
- no complete 10,000-page content/UI emission;
- no browser, crawler, accessibility, search, or commercial field evidence.

## 7. Files

- `26-graph-learning-paper-triage-and-promotion-gates.md`
- `25-academic-crosswalk-agent-harness-structured-generation-and-acceleration.md`
- `reference/src/ontology-graph.ts`
- `reference/src/opportunity-space-production.ts`
- `reference/src/acceleration-decision.ts`
- `memory/2026-07-18-0105-graph-learning-paper-triage.md`
- `memory/MEMORY.md`
