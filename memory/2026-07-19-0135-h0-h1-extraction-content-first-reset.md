# 2026-07-19 01:35 ET — H0/H1 Extraction and Content-First Reset

Status: immutable handoff
Branch: `agent/glm-blackwell-vertical-slice`

## Implemented

- moved the content-neutral compiler cluster into `hyper-site/src`;
- added a real TypeScript build to `@amtech/hyper-site`;
- changed the public entrypoint to package-owned `dist/index.js`;
- added complete root TypeScript declarations through `dist/index.d.ts`;
- converted matching `reference/src` modules into compatibility consumers;
- made reference builds compile Hyper Site first;
- added clean-room runtime, rejection and TypeScript tarball consumers;
- added `scripts/run-h0-h1-proof.mjs` and CI artifact upload;
- updated the product-boundary checker to package-owned authority;
- added the H0-H6 content-first reinvention program.

## H0 definition

All current streams integrate, clean-room consumers pass, a bounded compiler-limit run passes, and the exact commit produces a measured report. Failure means repair only.

## H1 definition

The public compiler cluster is physically package-owned and preserves accepted output, ordering, hashes and rejection behavior. The public package and declarations must install outside the monorepo without `reference/` access.

## Strategic correction

The conventional static framework floor is retained as a falsification and reliability substrate, not the product end-state.

First-class end-state hypotheses:

- autonomous semantic generation;
- self-aware GenUI and multi-surface projections;
- remote agent and browser task execution;
- SDRT and GNN relationship intelligence;
- GPU execution;
- Zig and Wasm pure-kernel or sandboxed acceleration.

These hypotheses are active, but none may bypass compiler validation, create a second semantic authority or own credentials/publication by default.

## Next gate

Wait for exact-head H0/H1 CI. Repair only measured failures. On green, begin H2 with one real approved business repository, five complete pages and a 25-page noindex cohort.
