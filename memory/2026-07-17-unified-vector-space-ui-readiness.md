# Memory — Unified Vector Space and UI Readiness

Date: 2026-07-17
Branch: `agent/phase-2-materialization-engine-plan`

## Decision

The five-slice Request Mirror manifest and synthetic mass-emission compiler are no longer parallel systems.

`GTM-RESEARCH/website-framework/site-manifest.yaml` is the sole declarative corpus authority. `reference/src/manifest.ts` composes it into the first-class hyper-vector space, derives graph neighbors, produces `SiteSource`, and compiles deterministic emissions.

## Reality

- The framework is source-wired research code, not a production web framework.
- The prior 2,000-page result was a synthetic compiler benchmark.
- All manifest pages remain noindex because `production_ready=false`.
- No real ranking, indexing, conversion, revenue, Cloudflare, browser, Zig, or Wasm acceptance exists.

## First-class geometry

Feature atoms and prototype sets precede modules and HTML. The geometry feeds:

- page packed vectors;
- internal-link suggestions;
- profile/page grouping;
- agent generation context;
- UI scaffold requirements;
- final page/instruction emissions.

## Agent harness

Agents propose typed page additions. Proposals require a target region, marginal-coverage hypothesis, information object, evidence, semantic modules, design capabilities, and provenance. They enter noindex at the research gate.

## Validation

GitHub Actions workflow `Website Framework Reference` run `29570420797` passed on head `72637e1e60179f9bac115b8b0c55c2b9f01d05f3`:

- dependency installation;
- TypeScript build and Node tests;
- unified manifest emission;
- emitted fixture artifact upload.

The artifact `website-framework-manifest-emissions` was uploaded with digest `sha256:0a2e6d843ac671913bed4c39efbab28a84b81affa0013bd29e765702e9b3b735`.

## Next pass

The user will provide a design system. Generate or consume `reference/generated-manifest/ui-scaffold.json`, map the supplied design system into a satisfactory semantic superset, implement the renderer adapter, and validate neutral-renderer parity before any field publication.
